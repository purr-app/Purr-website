---
title: Dynamic variables
description: Let one response feed the next request.
order: 4
---

## Extract and reuse

Extract values from a response and reuse them anywhere else in the workspace.

No scripts just to pass an access token, ID, or generated value from one request into another.

## The workflow

Response → extract value → dynamic variable → reuse in another request.

[See the Variables UI](/#variables).
